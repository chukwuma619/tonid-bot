{
  "name": "tonid-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev:website": "pnpm --filter @tonid-bot/website dev",
    "dev:bot": "pnpm --filter @tonid-bot/telegram-bot dev",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
